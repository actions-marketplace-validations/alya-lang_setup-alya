name: Test setup-alya

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    name: Test on ${{ matrix.os }} (${{ matrix.version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        version: ['latest', '0.0.19']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Alya Compiler (${{ matrix.version }})
        id: alya
        uses: ./
        with:
          version: ${{ matrix.version }}

      - name: Verify alya executable on PATH
        run: |
          alya --version

      - name: Check action outputs
        shell: bash
        run: |
          echo "Output version: ${{ steps.alya.outputs.version }}"
          echo "Output path:    ${{ steps.alya.outputs.alya-path }}"
          test -n "${{ steps.alya.outputs.version }}"
          test -n "${{ steps.alya.outputs.alya-path }}"

      - name: Test simple Alya execution
        shell: bash
        run: |
          echo 'say "Hello from setup-alya CI!"' > hello.alya
          alya run hello.alya

  test-arm:
    name: Test on ${{ matrix.os }} (latest)
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        # ARM binaries ship with post-0.0.19 releases, so only 'latest' is tested here
        os: [ubuntu-24.04-arm, windows-11-arm]
        version: ['latest']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Alya Compiler (${{ matrix.version }})
        id: alya
        uses: ./
        with:
          version: ${{ matrix.version }}

      - name: Verify alya executable on PATH
        run: |
          alya --version

      - name: Test simple Alya execution
        shell: bash
        run: |
          echo 'say "Hello from setup-alya ARM CI!"' > hello.alya
          alya run hello.alya
